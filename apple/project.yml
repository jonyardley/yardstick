name: Yardstick
options:
  bundleIdPrefix: com.yardley
packages:
  Shared: { path: ./generated/Shared }
  App: { path: ./generated/App }
targets:
  Yardstick:
    type: application
    platform: macOS
    deploymentTarget: "15.0"
    sources: [Yardstick]
    dependencies:
      - package: Shared
      - package: App
    settings:
      base:
        PRODUCT_NAME: Yardstick
        SWIFT_VERSION: "5.0"
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        # Ad-hoc local signing so headless xcodebuild (and CI, Task 9) works
        # without a signing identity. No App Sandbox / entitlements in this
        # dev phase — the embedded MCP server binds localhost directly;
        # sandbox + hardened runtime are a later-phase hardening item.
        CODE_SIGN_STYLE: Manual
        CODE_SIGN_IDENTITY: "-"
    info:
      path: Yardstick/Info.plist
      properties:
        CFBundleDisplayName: Yardstick
        NSHumanReadableCopyright: ""
  YardstickTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "15.0"
    sources: [YardstickTests]
    dependencies:
      - target: Yardstick
    settings:
      base:
        CODE_SIGN_STYLE: Manual
        CODE_SIGN_IDENTITY: "-"
        # Test bundles have no Info.plist source of their own; ad-hoc
        # signing (above) needs one to exist, so let xcodebuild synthesize
        # a minimal one rather than hand-authoring a second plist.
        GENERATE_INFOPLIST_FILE: YES
schemes:
  Yardstick:
    build:
      targets:
        Yardstick: all
    run:
      config: Debug
    test:
      config: Debug
      targets: [YardstickTests]
